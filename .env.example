# Пример файла .env для локальной разработки
# Скопируйте этот файл в .env и заполните свои значения
# ВАЖНО: .env не коммитится в git (добавлен в .gitignore)

# Railway Token (для деплоя через CI/CD)
# На Production это будет в GitHub Secrets
RAILWAY_TOKEN=your_railway_token_here

# FastAPI настройки
DEBUG=True
LOG_LEVEL=info

# Порт для локальной разработки
PORT=8000
HOST=0.0.0.0
