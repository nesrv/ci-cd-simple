Конечно 👍 Вот **краткая и понятная инструкция для студента**, без лишней теории.

---

# 📦 Railway CI/CD — ошибки деплоя и как их починить

## ❌ Ошибка 1: `Invalid RAILWAY_TOKEN`

**Причина:**
Неверный или устаревший токен Railway в CI.

**Решение:**

1. Зайти в Railway
2. `Account → Tokens` → создать новый токен
3. В GitHub:
   `Settings → Secrets → Actions → RAILWAY_TOKEN`
   вставить новый токен

---

## ❌ Ошибка 2: `Multiple services found`

**Причина:**
В проекте несколько сервисов, Railway не знает какой деплоить.

**Решение:**
Явно указать сервис:

```yaml
railway up --service web
```

(имя сервиса берётся из Railway)

---

## ❌ Ошибка 3: `railway is not recognized`

**Причина:**
Railway CLI не установлен локально.

**Решение (Windows):**

```powershell
npm install -g @railway/cli
```

Проверка:

```powershell
railway --version
```

---

## ❌ Ошибка 4: `Unauthorized. Please login`

**Причина:**
Нет локальной авторизации.

**Решение:**

```powershell
railway login
```

Проверка:

```powershell
railway whoami
```

---

## ❌ Ошибка 5: `No linked project found`

**Причина:**
Проект не привязан к локальной папке.

**Решение:**

```powershell
railway link
```

Выбрать:

* workspace
* project
* environment (`production`)
* service (`web`)

---

## 🐘 PostgreSQL в Railway (и в CI)

Приложение подключается к БД по переменным окружения (как в `docker-compose`):

- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`

**Локально:** поднять БД через Docker:

```bash
docker-compose up -d db
```

Схема и данные задаются в `scripts/init_db.sql` (при первом запуске контейнера Postgres подхватывает скрипты из `docker-entrypoint-initdb.d`).

**В CI (GitHub Actions):** в job `test` поднимается сервис `postgres:17` с теми же `POSTGRES_*`, затем выполняется `scripts/init_db.sql`, после чего запускаются тесты. Переменные `DB_*` в workflow совпадают с `docker-compose`.

**В Railway:**

1. В проекте Railway добавьте плагин **PostgreSQL** (New → Database → PostgreSQL).
2. У сервиса приложения (web) в **Variables** задайте переменные из созданной БД:
   - `DB_HOST` = хост из Railway (например `monolith.proxy.rlwy.net`)
   - `DB_PORT` = порт (например `12345`)
   - `DB_USER`, `DB_PASSWORD`, `DB_NAME` = из настроек Postgres в Railway

Либо, если Railway выдаёт одну строку `DATABASE_URL`, в приложении можно читать её и парсить (или задать отдельные переменные вручную).

---

## ✅ Итоговая правильная схема

### Локально (один раз):

```powershell
npm install -g @railway/cli
railway login
railway link
```

### В CI (обязательно):

```yaml
railway up --service web
```

---

## ⚠️ Важно помнить

* `railway login` — **только для локальной работы**
* `RAILWAY_TOKEN` — **только для CI**
* `.railway/` **не коммитится** в репозиторий

---

📌 Если деплой не работает — 90% проблем решаются этими шагами.
