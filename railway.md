Конечно 👍 Вот **краткая и понятная инструкция для студента**, без лишней теории.

---

# 📦 Railway CI/CD — ошибки деплоя и как их починить

## ❌ Ошибка 1: `Invalid RAILWAY_TOKEN`

**Причина:**
Неверный или устаревший токен Railway в CI.

**Решение:**

1. Зайти в Railway
2. `Account → Tokens` → создать новый токен
3. В GitHub:
   `Settings → Secrets → Actions → RAILWAY_TOKEN`
   вставить новый токен

---

## ❌ Ошибка 2: `Multiple services found`

**Причина:**
В проекте несколько сервисов, Railway не знает какой деплоить.

**Решение:**
Явно указать сервис:

```yaml
railway up --service web
```

(имя сервиса берётся из Railway)

---

## ❌ Ошибка 3: `railway is not recognized`

**Причина:**
Railway CLI не установлен локально.

**Решение (Windows):**

```powershell
npm install -g @railway/cli
```

Проверка:

```powershell
railway --version
```

---

## ❌ Ошибка 4: `Unauthorized. Please login`

**Причина:**
Нет локальной авторизации.

**Решение:**

```powershell
railway login
```

Проверка:

```powershell
railway whoami
```

---

## ❌ Ошибка 5: `No linked project found`

**Причина:**
Проект не привязан к локальной папке.

**Решение:**

```powershell
railway link
```

Выбрать:

* workspace
* project
* environment (`production`)
* service (`web`)

---

## ✅ Итоговая правильная схема

### Локально (один раз):

```powershell
npm install -g @railway/cli
railway login
railway link
```

### В CI (обязательно):

```yaml
railway up --service web
```

---

## ⚠️ Важно помнить

* `railway login` — **только для локальной работы**
* `RAILWAY_TOKEN` — **только для CI**
* `.railway/` **не коммитится** в репозиторий

---

📌 Если деплой не работает — 90% проблем решаются этими шагами.
