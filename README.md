# E-Shop API 🛒

Учебный проект FastAPI для электронного магазина видеокарт. Минималистичное приложение для изучения основных концепций REST API, работы с данными и управления корзиной.

## 📋 Требования

- **Python** 3.13+
- **FastAPI** - веб-фреймворк
- **Uvicorn** - ASGI-сервер
- **Pydantic** - валидация данных

## ⚙️ Установка

### 1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd CI-CD-SIMPLE
```

### 2. Создайте виртуальное окружение (рекомендуется)
```bash
python -m venv venv
```

#### Windows:
```bash
venv\Scripts\activate
```

#### Linux/macOS:
```bash
source venv/bin/activate
```

### 3. Установите зависимости
```bash
pip install fastapi uvicorn pydantic
```

Или через requirements.txt (если его создать):
```bash
pip install -r requirements.txt
```

## 🚀 Запуск приложения

### Запуск с автоперезагрузкой (разработка)
```bash
python main.py
```

Или напрямую с uvicorn:
```bash
uvicorn main:app --reload
```

### Запуск для производства
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

## 📖 Описание API

Приложение доступно по адресу: **http://localhost:8000**

### Интерактивная документация
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### 📦 Товары

| Метод | Путь | Описание |
|-------|------|---------|
| GET | `/products` | Получить все товары |
| GET | `/product/{id}` | Получить товар по ID |
| GET | `/search?q=название` | Поиск товара по названию |

**Примеры:**
```bash
# Все товары
curl http://localhost:8000/products

# Товар с ID 0
curl http://localhost:8000/product/0

# Поиск по названию
curl http://localhost:8000/search?q=RTX
```

#### 🛒 Корзина

| Метод | Путь | Описание |
|-------|------|---------|
| GET | `/cart` | Содержимое корзины + итого |
| POST | `/cart/add?pid=0&qty=1` | Добавить товар в корзину |
| DELETE | `/cart` | Очистить корзину |

**Примеры:**
```bash
# Посмотреть корзину
curl http://localhost:8000/cart

# Добавить товар (ID 0, количество 2)
curl -X POST "http://localhost:8000/cart/add?pid=0&qty=2"

# Очистить корзину
curl -X DELETE http://localhost:8000/cart
```

#### 📋 Заказы

| Метод | Путь | Описание |
|-------|------|---------|
| POST | `/checkout` | Оформить заказ из корзины |
| GET | `/orders` | Список всех заказов |

**Примеры:**
```bash
# Оформить заказ
curl -X POST http://localhost:8000/checkout

# Получить все заказы
curl http://localhost:8000/orders
```

#### 🏥 Здоровье приложения

| Метод | Путь | Описание |
|-------|------|---------|
| GET | `/health` | Статус приложения |

```bash
curl http://localhost:8000/health
```

## 📁 Структура проекта

```
CI-CD-SIMPLE/
├── main.py           # Основное приложение FastAPI
├── agents.py         # Копия приложения (альтернатива)
├── shop.json         # База данных товаров
└── README.md         # Этот файл
```

## 📊 Структура данных

### Товар (Product)
```json
{
  "name": "NVIDIA GeForce RTX 5090",
  "price": 230000.00,
  "description": "Флагманская видеокарта 2026 года",
  "created_at": "2026-01-15T10:00:00Z"
}
```

### Элемент корзины
```json
{
  "name": "NVIDIA GeForce RTX 5090",
  "qty": 1,
  "price": 230000.00
}
```

### Заказ
```json
{
  "items": [
    {"name": "GPU1", "qty": 1, "price": 100000}
  ],
  "total": 100000,
  "date": "2026-02-08T10:30:00.123456"
}
```

## 🧪 Примеры использования

### Сценарий 1: Поиск и покупка товара

```bash
# 1. Ищем RTX 5090
curl http://localhost:8000/search?q=RTX%205090

# 2. Добавляем в корзину (предположим ID = 0)
curl -X POST "http://localhost:8000/cart/add?pid=0&qty=1"

# 3. Проверяем корзину
curl http://localhost:8000/cart

# 4. Оформляем заказ
curl -X POST http://localhost:8000/checkout

# 5. Смотрим заказ
curl http://localhost:8000/orders
```

### Сценарий 2: Работа с несколькими товарами

```bash
# Добавляем несколько товаров
curl -X POST "http://localhost:8000/cart/add?pid=0&qty=1"
curl -X POST "http://localhost:8000/cart/add?pid=1&qty=2"

# Проверяем итого в корзине
curl http://localhost:8000/cart
```

## 🛠️ Особенности

- ✅ Асинхронные эндпоинты (async/await)
- ✅ Типизация Python 3.13+ (встроенные типы `list[dict]`)
- ✅ Валидация данных через Pydantic
- ✅ Интерактивная документация Swagger UI
- ✅ Обработка ошибок HTTP
- ✅ Поиск по названию товара
- ✅ Управление корзиной и заказами

## 💾 Данные

Все товары загружаются из файла `shop.json` при старте приложения.

**Имеется 10 моделей видеокарт:**
- NVIDIA GeForce RTX 5090
- NVIDIA GeForce RTX 4090
- AMD Radeon RX 9070 XT
- И другие...

## 🐛 Отладка

### Мониторинг запросов
```bash
# С подробными логами
uvicorn main:app --reload --log-level debug
```

### Проверка здоровья
```bash
curl http://localhost:8000/health
```

## 📝 Заметки для разработке

- Корзина и заказы хранятся в памяти (теряются при перезагрузке)
- Для продакшена используйте реальную БД (PostgreSQL, MongoDB и т.д.)
- ID товаров начинаются с 0
- Формат дат: ISO 8601

## 📚 Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Pydantic](https://docs.pydantic.dev/)
- [Uvicorn](https://www.uvicorn.org/)
- [REST API лучшие практики](https://restfulapi.net/)

## 📄 Лицензия

Этот проект создан в учебных целях.
